import React from 'react'

const Form = () => {

    const Input = ({
        className,
        type,
        name,
        value,
        inputClassName,
        onChange,
        onBlur,
        placeholder,
        error,
    }) => {}


    return (
        <div className='text-3xl text-stone-200'>Form</div>
    )
}

export default Form